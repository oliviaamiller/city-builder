export function createCountString(skylineCount, waterfrontCount, castleCount) {
    return `the city has changed ${skylineCount} times, the waterfront has changed ${waterfrontCount} times, the castle has changed ${castleCount} times.`;
}
